// "use strict";

// import ENV from "./environment.web";
// // declare var jest: any

// describe("Framework can use the generated environment.web.ts", () => {
//   it("can be imported with db initialized...", async () => {
//     return ENV.TransmuteFramework.db
//       .collection("RBACFactory")
//       .get()
//       .then(querySnapshot => {
//         querySnapshot.forEach(doc => {
//           console.log(`${doc.id} => `, doc.data());
//         });
//       });
//   });
// });
